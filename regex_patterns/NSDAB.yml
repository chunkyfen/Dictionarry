description: 'Matches the release group `NSDAB` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: NSDAB
pattern: (?<=^|[\s.-])NSDAB\b
tags:
- Release Group
- Anime
tests: []
