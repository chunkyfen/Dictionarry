description: 'Matches the release group `Cunnysseur` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Cunnysseur
pattern: (?<=^|[\s.-])Cunnysseur\b
tags:
- Release Group
- Anime
tests: []
