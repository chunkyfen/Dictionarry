description: 'Matches the release group `0x539` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: 0x539
pattern: (?<=^|[\s.-])0x539\b
tags:
- Release Group
- Anime
tests: []
