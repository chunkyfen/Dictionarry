description: 'Matches the release group `JySzE` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: JySzE
pattern: (?<=^|[\s.-])JySzE\b
tags:
- Release Group
- Anime
tests: []
