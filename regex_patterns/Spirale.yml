description: 'Matches the release group `Spirale` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Spirale
pattern: (?<=^|[\s.-])Spirale\b
tags:
- Release Group
- Anime
tests: []
