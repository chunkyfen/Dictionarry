description: 'Matches the release group `Shimatta` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Shimatta
pattern: (?<=^|[\s.-])Shimatta\b
tags:
- Release Group
- Anime
tests: []
