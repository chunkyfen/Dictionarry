description: 'Matches the release group `Smoke` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Smoke
pattern: (?<=^|[\s.-])Smoke\b
tags:
- Release Group
- Anime
tests: []
