description: 'Matches the release group `Brrrrrrr` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Brrrrrrr
pattern: (?<=^|[\s.-])Brrrrrrr\b
tags:
- Release Group
- Anime
tests: []
