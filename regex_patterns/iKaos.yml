description: 'Matches the release group `iKaos` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: iKaos
pattern: (?<=^|[\s.-])iKaos\b
tags:
- Release Group
- Anime
tests: []
