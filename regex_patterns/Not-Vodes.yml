description: 'Matches the release group `Not-Vodes` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Not-Vodes
pattern: (?<=^|[\s.-])Not-Vodes\b
tags:
- Release Group
- Anime
tests: []
