description: 'Matches the release group `DemiHuman` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: DemiHuman
pattern: (?<=^|[\s.-])DemiHuman\b
tags:
- Release Group
- Anime
tests: []
