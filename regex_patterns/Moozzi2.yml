description: 'Matches the release group `Moozzi2` only if it is:


  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period
  (`.`), or a hyphen (`-`).

  - Followed by a word boundary (`\b`), ensuring it ends cleanly without being part
  of a longer word. '
name: Moozzi2
pattern: (?<=^|[\s.-])Moozzi2\b
tags:
- Release Group
- Anime
tests: []
