description: 'Matches the release group `Metal` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Metal
pattern: (?<=^|[\s.-])Metal\b
tags:
- Release Group
- Anime
tests: []
