description: 'Matches the release group `Datte13` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: Datte13
pattern: (?<=^|[\s.-])Datte13\b
tags:
- Release Group
- Anime
tests: []
