description: 'Matches the release group `CUNNY` only if it is:

  - Preceded by the start of the string (`^`), a whitespace character (`\s`), a period (`.`), or a hyphen (`-`).
  - Followed by a word boundary (`\b`).'
name: CUNNY
pattern: (?<=^|[\s.-])CUNNY\b
tags:
- Release Group
- Anime
tests: []
